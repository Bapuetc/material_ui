import React from 'react';
import ReactDOM from 'react-dom';
import Containr from './Container';

ReactDOM.render(
  <React.StrictMode>
    <Containr />
  </React.StrictMode>,
  document.getElementById('root')
);
